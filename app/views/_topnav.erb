<div class="navbar navbar-inverse navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <a class="brand" href="<%= url_for(:index) %>"><strong>ROGv</strong> <small>- Forts Watching System (for <%= ROGv::ServerSettings.env.server_name %>)</small></a>
      <ul class="nav pull-right">
        <li class="dropdown">
          <a href="#" class="dropdown-toggle" data-toggle="dropdown">Strategy Room <b class="caret"></b></a>
          <ul class="dropdown-menu">
            <li><%= link_to('Timeline', url_for(:t, :date_list)) %></li>
            <li><%= link_to('Result', url_for(:r, :menu)) %></li>
          </ul>
        </li>
        <% if updatable_mode? %>
          <li><%= link_to('Admin Tools', url_for(:a, :login)) %></li>
        <% end %>
      </ul>
    </div>
  </div>
</div>
